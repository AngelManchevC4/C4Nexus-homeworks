<isdecorate template="common/layout/page">
    <isif condition="${!session.custom.x}">
        <isset name="x" value="${1}" scope="session" />
        ${session.custom.x}
    <iselse/>
        ${session.custom.x++}
    </isif>
</isdecorate>